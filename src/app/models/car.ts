export interface Car {

  id: number;
    car_year: number;
    licensePlate: string;
    model: string;
    color: string;
    quantUsuario: number;
    userId: number; // O ID do usuário associado
}
